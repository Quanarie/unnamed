generator client {
  provider = "prisma-client-js"
}

datasource prisma {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Phrase {
  id   Int    @id @default(autoincrement())
  content String
}
