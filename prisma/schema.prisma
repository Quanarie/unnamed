generator client {
  provider = "prisma-client-js"
}

datasource prisma {
  provider = "sqlite"
  url      = "file:./database.db"
}

model Phrase {
  id   Int    @id @default(autoincrement())
  content String
}